import 'package:flutter/widgets.dart';

class Not extends StatelessWidget {
  const Not({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print(['context', context, this]);
    return Container();
  }
}
